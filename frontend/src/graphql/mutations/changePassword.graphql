mutation changePassword(
  $token: String!
  $newPassword: String!
  $confirmPassword: String!
) {
  changePassword(
    newPassword: $newPassword
    confirmPassword: $confirmPassword
    token: $token
  ) {
    errors {
      ...RegularError
    }
    user {
      id
    }
  }
}
